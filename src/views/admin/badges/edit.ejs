<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/admin/badges/edit.css">
    <title><%= badge._id %> @ Badge Editor</title>
</head>
<body>
    <h1>Edit Badge</h1>
    <div class="container">
        <form method="POST" action="/admin/badges/edit/<%= badge._id %>">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" value="<%= badge.name %>" required>
            </div>
            
            <div class="form-group">
                <label for="range">Range</label>
                <input type="text" id="range" name="range" value="<%= badge.bitpoints_min %> - <%= badge.bitpoints_max %>" readonly>
            </div>
    
            <div class="form-group">
                <label for="bitpoints_min">Min Points</label>
                <input type="number" id="bitpoints_min" name="bitpoints_min" value="<%= badge.bitpoints_min %>" min="0" required>
            </div>
    
            <div class="form-group">
                <label for="bitpoints_max">Max Points</label>
                <input type="number" id="bitpoints_max" name="bitpoints_max" value="<%= badge.bitpoints_max %>" min="0" required>
            </div>
    
            <div class="form-group">
                <label for="image_url">Image URL</label>
                <input type="text" id="image_url" name="image_url" value="<%= badge.image_url %>" required>
            </div>
    
            <div class="form-buttons">
                <button class="save" type="submit">Save</button>
                <a href="/admin/badges"><button class="cancel" type="button">Cancel</button></a>
            </div>
        </form>
    </div>
</body>
</html>